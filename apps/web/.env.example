# Copia este archivo a `apps/web/.env.local` y ajusta lo que necesites.
# `.env.local` esta en .gitignore: nunca se versiona.

# A donde reescribe Next las peticiones /api/*. Es una URL de servidor a
# servidor: el navegador nunca la ve.
API_ORIGIN=http://127.0.0.1:4000
